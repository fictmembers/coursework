<%= render 'layouts/header' %>

	<div class="row">
		<div class="col s10 offset-s1">
			<table id="manager">
			    <thead>
			          <tr>
										<th data-field="id">ID</th>
			              <th data-field="lastname">Lastname</th>
			              <th data-field="restaurant">Restaurant</th>
			              <th data-field="created_at">Created at</th>
				            <th data-field="edit">Edit</th>
		    				    <th data-field="delete">Delete</th>
			          </tr>
			    </thead>

			    <tbody>
			    	<% @managers.each do |manager| %>
						<% restaurant = Restaurant.find(manager.restaurant_id) %>
			          <tr>
									<td><%= manager.id %></td>
			            <td><%= manager.lastname %></td>
			            <td><%= restaurant.name %></td>
			            <td><%= manager.created_at %></td>
                  <td><%= link_to 'Edit', edit_manager_path(manager) %></td>
	    					  <td><%= link_to "Delete", manager, method: :delete, data: { confirm: "You sure?" } %></td>
			          </tr>
			        <% end %>
			    <tbody>
			</table>
		</div>
	</div>
