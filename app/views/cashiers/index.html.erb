<%= render 'layouts/header' %>

	<div class="row">
		<div class="col s10 offset-s1">
			<table id="cashier">
			    <thead>
			          <tr>
										<th data-field="id">ID</th>
			              <th data-field="lastname"> <%=t :name_lastname %></th>
			              <th data-field="restaurant"> <%=t :restaurant %></th>
										<th data-field="created_at"> <%=t :created %></th>
                    <th data-field="edit"> <%=t :edit %></th>
		    				    <th data-field="delete"> <%=t :delete %></th>
			          </tr>
			    </thead>

			    <tbody>
			    	<% @cashiers.each do |cashier| %>
						<% restaurant = Restaurant.find(cashier.restaurant_id) %>
			          <tr>
									<td> <%= cashier.id %></td>
			            <td> <%= cashier.lastname %></td>
			            <td> <%= restaurant.name %></td>
									<td> <%= cashier.created_at %></td>
			            <td> <%= link_to "#{t('edit')}", edit_cashier_path(cashier) %></td>
	    					  <td> <%= link_to "#{t('delete')}", cashier, method: :delete, data: { confirm: "#{t('you_sure')}" } %></td>
			          </tr>
			        <% end %>
			    <tbody>
			</table>
		</div>
	</div>
