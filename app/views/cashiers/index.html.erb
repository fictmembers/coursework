<%= render 'layouts/header' %>

	<div class="row">
		<div class="col s10 offset-s1">
			<table id="cashier">
			    <thead>
			          <tr>
										<th data-field="id">ID</th>
			              <th data-field="lastname">Lastname</th>
			              <th data-field="restaurant">Restaurant</th>
										<th data-field="created_at">Created at</th>
                    <th data-field="edit">Edit</th>
		    				    <th data-field="delete">Delete</th>
			          </tr>
			    </thead>

			    <tbody>
			    	<% @cashiers.each do |cashier| %>
						<% restaurant = Restaurant.find(cashier.restaurant_id) %>
			          <tr>
									<td><%= cashier.id %></td>
			            <td><%= cashier.lastname %></td>
			            <td><%= restaurant.name %></td>
									<td><%= cashier.created_at %></td>
			            <td><%= link_to 'Edit', edit_cashier_path(cashier) %></td>
	    					  <td><%= link_to "Delete", cashier, method: :delete, data: { confirm: "You sure?" } %></td>
			          </tr>
			        <% end %>
			    <tbody>
			</table>
		</div>
	</div>
