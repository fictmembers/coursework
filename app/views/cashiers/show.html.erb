<div class="formfactor">
  <h4><%= @cashier.lastname %></h4>
  Personal id: <%= @cashier.id %></br>
  Works at <%= @restaurant.name %> since <%= @cashier.created_at.strftime("%d.%b.%Y") %> </br>
</div>
